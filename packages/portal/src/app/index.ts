/**
 * Package entry stubs — live Phase 3 shell is in the root SPA (`src/app`).
 * These re-exports keep the monorepo map aligned with the implementation docs.
 */
export { default as Shell } from '../../../src/app/Shell';
export { default as AuthGuard } from '../../../src/app/AuthGuard';
export { default as ImmersiveButton } from '../../../src/app/ImmersiveButton';
